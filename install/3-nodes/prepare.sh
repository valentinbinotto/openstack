#!/bin/bash

# Execute this script on a Bastion-host that is able to connect via SSH to all nodes
# Make sure the appropriate network infrastructure is already in place
# Use Ubuntu as OS for the OpenStack nodes if possible. You have to use a 64-bit OS
#
# Make sure each Node has at least 1 processor/core, 4 GB memory and 20 GB storage
#
# Run commands if not explicitly defined otherwise as root



